# SysIn V1.2 Native Windows Slice — Workstream 1 Evidence Review

**Review date:** 2026-09-09  
**Branch:** `impl/v1.2-w1-native-core-windows`  
**Candidate commit:** `78ea902969b64ea26eecc79d95fe4328d179d3d4`  
**RID:** `win-x64`  
**Version identity:** `SysIn 1.2.0-preview.1` / `Channel: Preview`  
**Verdict:** **HOLD — owner-PC/live visual acceptance still required**

This review applies the Workstream-1 rule that every applicable criterion must be exactly 10.0/10 and every required verification must be complete before PASS. Green CI is necessary but not sufficient. Unverified live-terminal, real-PC performance, and owner visual-acceptance requirements keep the slice on HOLD.

## 1. Exact candidate evidence

### GitHub Actions

- `native-preview` run: `34332244540` — **SUCCESS**
  - Release restore/build: PASS
  - Native tests: **55/55 PASS**, 0 failed, 0 skipped
  - Self-contained `win-x64` publish: PASS
  - Preview CLI/evidence validation: PASS
  - Seven Overview render captures uploaded: PASS
  - Native preview + evidence artifact uploaded: PASS
  - V1.1 regression in PowerShell 7: PASS
  - V1.1 regression in Windows PowerShell 5.1: PASS
- Existing unchanged `test.yml` run: `34332244464` — **SUCCESS**
  - PowerShell 7 parse/behavior tests: PASS
  - Windows PowerShell 5.1 parse/behavior tests: PASS

### Published artifact identity

- GitHub artifact `native-preview-win-x64`
  - Artifact ID: `10096274884`
  - Artifact ZIP digest: `sha256:c41ec6922e27f19b5fd8c9bdb849a8d86a54e29d93ff11131f86744358c05d71`
- GitHub artifact `native-overview-render-captures`
  - Artifact ID: `10096272420`
  - Artifact ZIP digest: `sha256:9c43dff672642b62cb034e6fac260fab455a4dea34aaaba96c8895fe9909e601`
- Published `SysIn.App.exe` SHA-256 recorded by CI and independently recomputed from the downloaded artifact:
  - `fd74d573fc597580701cb4bfffa0788724ac144d37dd43f162696401b4903cf2`

### CLI evidence

`build.txt` records:

```text
commit=78ea902969b64ea26eecc79d95fe4328d179d3d4
rid=win-x64
doctorExit=0
overviewNoninteractiveExit=2
```

`version.txt` records:

```text
SysIn 1.2.0-preview.1
Channel: Preview
Runtime: Native .NET 10
Platform: win-x64
```

`doctor.txt` on the Windows hosted runner records:

```text
Runtime: PASS — .NET 10
Windows discovery: PASS — 17 devices visible
Collectors: PASS — 5 reporting healthy
Terminal: Tier0 noninteractive
Renderer: Preview Overview registered; live visual certification is separate.
```

The redirected JSON snapshot parses successfully as schema `1.0-preview`. Unsupported GPU utilization is represented as `value:null`, `status:"unsupported"`, `freshness:"notApplicable"`; no zero value is fabricated.

## 2. Deterministic Overview render review

Required captures were generated for:

- 80×24
- 100×30
- 120×35
- 140×40
- 160×45
- 180×50
- 200×60

### Defects found and corrected during review

1. **100×30 command-bar clipping** — previous capture truncated `[Q] Quit` to `[Q] Q`. A regression test was added and the renderer changed to keep the footer complete.
2. **Unimplemented key advertising** — previous footer advertised page/search/help keys that the live loop did not implement. A regression test was added; Preview now advertises only `[P] Pause  [F] FPS  [Q] Quit`.
3. **GPU/lower-card geometry collision** — stacked GPU panels previously overlapped the lower Memory/Storage/Network region and produced a visible `─│` artifact. A RED regression test reproduced the exact artifact. The renderer now gives CPU/GPU an explicit upper-height budget and starts lower cards strictly after that region.

The fresh captures from commit `78ea9029...` no longer contain the prior `─│` overlap artifact. All seven footer lines are complete and truthful.

## 3. Criterion-by-criterion status

| Criterion | Evidence | Status |
|---|---|---|
| .NET 10 Release restore/build | exact CI run | **10.0/10** |
| Native automated tests | 55/55 on exact candidate | **10.0/10** |
| Unchanged V1.1 automated regression | both PowerShell hosts + existing workflow green | **10.0/10** |
| Native self-contained publish | exact `win-x64` artifact produced | **10.0/10** |
| Native CLI launch/version/help/snapshot/doctor | published executable exercised in CI | **10.0/10** |
| Redirected JSON contract | parses; explicit null/status semantics | **10.0/10** |
| Honest unsupported GPU metric | `null` + Unsupported/NotApplicable, never fake zero | **10.0/10** |
| Collector health on CI Windows machine | 5/5 healthy | **10.0/10** |
| Deterministic responsive rendering | seven required captures; renderer tests pass | **10.0/10** |
| 100×30 footer completeness | regression test + fresh capture | **10.0/10** |
| Preview control truthfulness | only P/F/Q advertised | **10.0/10** |
| GPU/lower-card non-overlap | RED→GREEN regression + fresh capture inspection | **10.0/10** |
| Structural screen utilization | automated guard + captures | **10.0/10 for structural slice** |
| Render+diff performance smoke | bounded CI smoke remains below required 10 ms threshold | **10.0/10 for bounded smoke only** |
| Real interactive terminal lifecycle | automated virtual-terminal coverage exists, but exact candidate not yet exercised interactively on owner PC | **UNVERIFIED — HOLD** |
| Real Q/P/F behavior | implementation/tests exist; owner-PC live interaction not yet observed | **UNVERIFIED — HOLD** |
| Real terminal restoration after Q/Esc/Ctrl+C | automated coverage exists; owner-PC live restoration not yet observed | **UNVERIFIED — HOLD** |
| Real actual-vs-target FPS behavior | model/loop tests exist; sustained live owner-PC evidence not yet captured | **UNVERIFIED — HOLD** |
| Real CPU/RAM/working-set overhead | no owner-PC steady-state measurement yet | **UNVERIFIED — HOLD** |
| Real multi-GPU identity on Doonchy hardware | deterministic dual-GPU fixture exists; hosted runner only exposes Hyper-V GPU; owner hardware not yet exercised | **UNVERIFIED — HOLD** |
| Live color richness / Spectrum polish | plain-text captures cannot certify truecolor appearance | **UNVERIFIED — HOLD** |
| Real compact/normal/maximized resize behavior | deterministic sizes pass; live resize/reflow not yet observed | **UNVERIFIED — HOLD** |
| Owner visual acceptance | Doonchy has not yet reviewed this exact candidate live | **UNVERIFIED — HOLD** |
| Installed V1.1 remains untouched on owner PC | side-by-side artifact does not install/replace V1.1; owner-PC before/after confirmation still required | **UNVERIFIED — HOLD** |

## 4. Required owner-PC acceptance run

Use the exact downloaded/repacked candidate whose executable SHA-256 is:

`fd74d573fc597580701cb4bfffa0788724ac144d37dd43f162696401b4903cf2`

Before running Preview, record installed V1.1 identity with the normal installed command. Run the Preview only from its extracted side-by-side folder; do not copy it into the V1.1 installation and do not alter PATH.

Required observations for Workstream-1 PASS:

1. `SysIn.App.exe version` shows `1.2.0-preview.1`, Preview, Native .NET 10, win-x64.
2. `SysIn.App.exe doctor` is reviewed; degraded optional telemetry is acceptable only when represented honestly and explained.
3. `SysIn.App.exe snapshot` and `snapshot --json` show the actual machine's CPU, memory, volumes, network, uptime, and every GPU identity; GPU utilization may remain N/A in this workstream.
4. Interactive `SysIn.App.exe overview` opens and restores the terminal cleanly.
5. P pauses/resumes collection; F cycles 20→10→5→1→20; Q/Esc quits.
6. Resize through compact, normal, and maximized terminal sizes; no clipping, collisions, stale artifacts, or top-left-island layout.
7. Confirm color richness, hierarchy, legibility, spacing, and overall polish at realistic viewing size.
8. Observe actual vs target FPS for sustained operation.
9. Measure steady SysIn CPU usage and working set while the dashboard is running.
10. Re-run the installed V1.1 `sysin version` afterward and confirm it remains unchanged.

## 5. Final Workstream-1 verdict

**HOLD.**

The exact candidate is engineering-green and deterministic-render clean for the defects found so far. It is **not** Workstream-1 PASS because the completion contract explicitly requires realistic live visual review, owner-PC telemetry/interaction/performance evidence, terminal restoration, resize behavior, and Doonchy acceptance. Under the 10.0/10 rule, unknown or unverified requirements cannot be averaged away.

**Do not begin Workstream 2 until this review is updated to PASS after owner approval.**
